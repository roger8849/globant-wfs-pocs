version: '2'
services:
  db:
      image: postgres:9.5
      ports:
        - "5432:5432"
      environment:
        - POSTGRES_DB=samples
        - POSTGRES_USER=postgres
        - POSTGRES_PASSWORD=postgres